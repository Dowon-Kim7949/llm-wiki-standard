---
title: 릴리스 노트 v1.8.0 · Release Notes v1.8.0
tags:
  - llm-wiki
  - release-notes
status: needs_review
doc_type: release_notes
project: llm-wiki-standard
last_updated: 2026-07-15
author: ai-generated
last_edited_by: Claude Code
wiki_block_version: v1
source_files:
  - package.json
  - src/config-file.js
  - src/commands.js
related:
  - docs/llm-wiki/log.md
visibility: internal
contains_sensitive_info: false
---

# 릴리스 노트 v1.8.0 · Release Notes v1.8.0

_생성일 2026-07-15. 사람 검토 전 `needs_review`. · Generated 2026-07-15; `needs_review` pending human review._

## 요약 · Summary

- config 스키마 확장의 첫 피처 슬라이스: per-project rule 토글(Gate 13). 1.7.2 enabling-prep 위에 세웠다. Additive·opt-in, 1.0.0 계약·zero-dep 불변. · First feature slice of config schema growth: per-project rule toggles (Gate 13), built on the 1.7.2 enabling prep. Additive/opt-in; contracts and zero-dependency unchanged.

## 추가 · Added

- `llm-wiki.config.json`의 `rules` 맵 — 개별 finding rule을 끄거나(`off`) severity를 재정의한다(`blocked/error/warning/info`). `audit`/`status`/`validate-frontmatter`에 중앙(`applyRuleConfig`) 적용되고 CLI·API·MCP 모두 반영. 레지스트리 rule만 대상, **`sensitive.*`(민감정보)는 절대 토글 불가**. · a `rules` map that turns a finding rule off or overrides its severity, applied centrally across audit/status/validate-frontmatter and all three surfaces; sensitive-info rules are never toggleable.
- `content.thin_body` — 기본 off인 opt-in enrichment lint. `rules`로 켜면 본문 prose가 얇은 문서를 표시한다(토글 기계 dogfood). · an opt-in, off-by-default lint that flags thin-body docs when enabled; dogfoods the toggle machinery.
- `doctor`가 활성 rule 토글 수를 echo한다. · doctor echoes the active toggle count.

## 참고 · Notes

- severity 레지스트리 수렴 pre-work는 감사로 동작 보존 확인(push 지점·레지스트리 불일치 0). 커스텀 문서셋·템플릿 오버라이드는 `1.8.x`로 후속. 범위: GATE_REVIEW Gate 13(accepted). · The severity-registry consolidation was audited behavior-preserving (0 mismatches); custom document sets and template overrides follow in 1.8.x. Scope: Gate 13 (accepted).
- 사람 검토 전까지 `needs_review`로 유지한다. 지식 문서 3개(PUBLIC_API/DOMAIN_FEATURES/ARCHITECTURE_CONVENTIONS)가 doc-sync로 needs_review가 됐다(배포 후 재검토). · Kept `needs_review`; three knowledge docs dropped to needs_review via doc-sync (re-verify after deploy).
