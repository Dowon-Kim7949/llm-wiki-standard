---
title: 릴리스 노트 v1.7.1 · Release Notes v1.7.1
tags:
  - llm-wiki
  - release-notes
status: needs_review
doc_type: release_notes
project: llm-wiki-standard
last_updated: 2026-07-15
author: ai-generated
last_edited_by: Claude Code
wiki_block_version: v1
source_files:
  - package.json
  - src/commands.js
  - .gitattributes
related:
  - docs/llm-wiki/log.md
visibility: internal
contains_sensitive_info: false
---

# 릴리스 노트 v1.7.1 · Release Notes v1.7.1

_생성일 2026-07-15. 사람 검토 전 `needs_review`. · Generated 2026-07-15; `needs_review` pending human review._

## 요약 · Summary

- 패치(저장소 위생). CLI·JSON·프로그래매틱 API·frontmatter 계약과 런타임 동작 변경 없음. · Patch (repository hygiene). No CLI/JSON/programmatic-API/frontmatter contract or runtime behavior change.

## 수정 · Fixed

- `src/commands.js`의 `wikiGraph` 엣지 중복제거 키(`collectWikiGraph` → `addEdge`)에 날것의 `U+0000`(NUL) 제어 바이트가 구분자로 박혀 있었다. git `text=auto`가 이 파일을 바이너리로 분류해, 저장소 `.gitattributes`의 `eol=lf` 정규화에서 이 파일 하나만 제외되어 CRLF로 저장돼 있었다. 날것 바이트를 `\\u0000` 이스케이프로 교체하고 파일을 LF로 재정규화했다. · A raw `U+0000` (NUL) control byte sat in the `wikiGraph` edge-dedup key, so git `text=auto` classified `src/commands.js` as binary and it was the one source file exempt from the repo's `.gitattributes` `eol=lf` normalization (stored as CRLF). Replaced the raw byte with the `\\u0000` escape and renormalized the file to LF.

## 참고 · Notes

- 런타임 문자열은 불변이다: 템플릿 리터럴의 `\\u0000`은 동일한 NUL 코드포인트를 만들어 엣지 중복제거가 바이트 단위로 동일하다. 커밋 diff 대부분은 `src/commands.js`의 일회성 CRLF→LF 재정규화다. · Runtime string unchanged: `\\u0000` in the template literal yields the same NUL code point, so edge dedup is byte-identical. Most of the commit diff is the one-time CRLF→LF renormalization of `src/commands.js`.
- 사람 검토 전까지 `needs_review`로 유지한다. · Kept `needs_review` pending human review.
