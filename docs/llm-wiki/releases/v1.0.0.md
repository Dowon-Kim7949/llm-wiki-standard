---
title: 릴리스 노트 v1.0.0 · Release Notes v1.0.0
tags:
  - llm-wiki
  - release-notes
status: needs_review
doc_type: release_notes
project: llm-wiki-standard
last_updated: 2026-07-14
author: ai-generated
last_edited_by: Claude Code
wiki_block_version: v1
source_files:
  - package.json
related:
  - docs/llm-wiki/log.md
visibility: internal
contains_sensitive_info: false
---

# 릴리스 노트 v1.0.0 · Release Notes v1.0.0

_생성일 2026-07-14. 사람 검토 전 `needs_review`. · Generated 2026-07-14; `needs_review` pending human review._

## 요약 · Summary

- `1.0.0`은 기능 릴리스가 아니라 **안정성 선언**이다. `0.1.8`의 명령 표면을 기능 변경 없이 그대로 승격한다. · `1.0.0` is a stability declaration, not a feature release: it promotes the `0.1.8` command surface with no functional changes.

## 안정 계약 · Stability

- CLI 명령·옵션 표면, `--format json` 출력 형태, 필수 frontmatter 계약을 안정 계약으로 확정했다. 이후 파괴적 변경은 major 버전 상승이 필요하다. · Declared the CLI command/option surface, `--format json` output shape, and the required frontmatter contract stable; breaking changes now require a major version bump.

## 추가 · Added

- 릴리스 품질 CI: Node 18.18.0 / 20 / 22 / 24 × Windows / macOS / Linux 매트릭스와 packed-tarball consumer install 스모크 테스트. · Release-quality CI: a Node 18.18.0 / 20 / 22 / 24 × Windows / macOS / Linux matrix and a packed-tarball consumer install smoke test.
- npm 패키지에 포함되는 루트 `CHANGELOG.md` 신설. · A new root `CHANGELOG.md` shipped in the npm package.

## 참고 · Notes

- 보수적 쓰기 정책은 그대로다: `--write` 없이는 쓰지 않고, `migrate --apply`는 차단을 유지하며, `log.md`와 기존 adapter 파일은 보존한다. · The conservative write policy is unchanged: no writes without `--write`, `migrate --apply` stays blocked, and `log.md` and existing adapter files are preserved.
