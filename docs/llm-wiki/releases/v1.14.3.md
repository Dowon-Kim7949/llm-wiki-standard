---
title: 릴리스 노트 v1.14.3 · Release Notes v1.14.3
tags:
  - llm-wiki
  - release-notes
status: needs_review
doc_type: release_notes
project: llm-wiki-standard
last_updated: 2026-07-20
author: ai-generated
last_edited_by: Claude Code
wiki_block_version: v1
source_files:
  - package.json
  - src/cli.js
  - src/commands.js
related:
  - docs/llm-wiki/log.md
visibility: internal
contains_sensitive_info: false
---

# 릴리스 노트 v1.14.3 · Release Notes v1.14.3

_생성일 2026-07-20. 사람 검토 전 `needs_review`. · Generated 2026-07-20; `needs_review` pending human review._

## 요약 · Summary

- 두 번째 노출 보고서 기반 온보딩 오리엔테이션. 처음 쓰는 사용자가 도구의 의도를 모르겠다고 했고, 한국 테스터가 한글 출력을 원했다. 사용자-대면 핵심 텍스트를 이중언어(KO+EN)로. 계약·zero-dep 불변. · Onboarding orientation from a second exposure report: a first-timer couldn't tell what the tool is for, and a Korean tester asked for Korean. Key user-facing text is now bilingual (KO+EN). Contracts and zero-dependency unchanged.

## 추가 · Added

- **`llm-wiki` / `--help` 이중언어 오리엔테이션 헤더** — Usage 나열 전에 "무엇을/왜/3단계 흐름(뼈대 생성 → 프롬프트를 에이전트에 붙여넣기 → 사람 검토·verified)"을 KO+EN으로 보여준다. · A bilingual what/why/3-step orientation now leads `--help` instead of the bare Usage list.
- **`--help`에 버전 + `@latest` 팁** — `llm-wiki vX.Y.Z` 표시 + `npx …@latest` 권장으로 npx 캐시가 옛 버전을 조용히 재사용하는 상황을 표면화. · Help shows the version and recommends `@latest`, making a stale npx cache noticeable.
- **`quickstart` 출력 `About · 소개` 라인**(이중언어) — help를 안 보고 quickstart를 바로 실행한 사용자도 방향을 잡게. · A bilingual `About · 소개` line on `quickstart` for users who skip `--help`.

## 참고 · Notes

- 배경: 두 번째 테스터가 `npx …@1.14.0`(npx 캐시)로 실행해 1.14.1의 `Ready` 리네임을 못 받은 채 옛 `Blocked`를 봤다 — `@latest` 팁·버전 표기는 이 캐시 혼란을 겨냥한다. · The tester hit a cached `@1.14.0` (so missed the 1.14.1 `Ready` rename); the `@latest` tip and version line target that confusion.
- 범위: 사용자-대면 오리엔테이션/help/quickstart About을 이중언어로. finding rule ID·정밀 리포트는 영문 유지. 전면 i18n(`--lang`)은 별도 결정 사항. · Scope: orientation/help/quickstart About are bilingual; finding rule IDs stay English. Full i18n (`--lang`) remains a separate decision.
- 검증: node --test 228 통과(신규 2), validate result:pass 0 findings, strict pass. bare 명령 오리엔테이션 실제 CLI 확인. · Verify: 228 tests pass (+2), validate result:pass (0 findings), strict pass; the bare-command orientation confirmed via the real CLI.
