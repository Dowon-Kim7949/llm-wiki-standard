---
title: 릴리스 노트 v1.15.0 · Release Notes v1.15.0
tags:
  - llm-wiki
  - release-notes
status: needs_review
doc_type: release_notes
project: llm-wiki-standard
last_updated: 2026-07-20
author: ai-generated
last_edited_by: Claude Code
wiki_block_version: v1
source_files:
  - package.json
  - src/commands/skills.js
  - src/cli.js
related:
  - docs/llm-wiki/log.md
visibility: internal
contains_sensitive_info: false
---

# 릴리스 노트 v1.15.0 · Release Notes v1.15.0

_생성일 2026-07-20. 사람 검토 전 `needs_review`. · Generated 2026-07-20; `needs_review` pending human review._

## 요약 · Summary

- 스킬 생성(Gate 21) — 위키-그라운디드 자동화 프롬프트를 각 에이전트 네이티브 형식으로 생성해, 생성한 위키가 실제 기능 작업에 쓰이도록 한다(가치 루프 #8 해소). 부가적·opt-in, 계약·zero-dep 불변. MINOR. · Skill generation (Gate 21): wiki-grounded automation prompts in each agent's native shape, so a generated wiki actually gets used for feature work (closes value-loop #8). Additive/opt-in, contracts and zero-dep unchanged. MINOR.

## 추가 · Added

- **`init`/`quickstart`이 자동화 프롬프트 아티팩트를 생성** — `feature`/`fix`/`docs-sync` 3종을 각각: Claude 스킬 `.claude/skills/llm-wiki-<task>/SKILL.md`, Cursor 룰 `.cursor/rules/llm-wiki-<task>.mdc`, 에이전트-중립 프롬프트 `.llm-wiki/prompts/llm-wiki-<task>.md`(Codex 등). · Generates automation-prompt artifacts for feature/fix/docs-sync as a Claude skill, a Cursor rule, and an agent-neutral prompt.
- **도메인 맵 주입** — 각 본문에 프로젝트 도메인 맵(`docs/llm-wiki/domains/` 스냅샷)을 넣어 에이전트가 어떤 문서를 먼저 읽을지 즉시 알게 한다(생성 시점 스냅샷; 갱신하려면 재생성). · Each body embeds a generation-time snapshot of the project's domain map.
- **`--skills` 플래그**(init/quickstart) + `--agent claude|cursor` 선택 시 자동 생성. opt-in·preview-first, 기존 파일 미덮어씀, recognize-don't-run(도구는 생성만, 실행은 에이전트), needs_review 규율 본문 내장. 스킬 미요청 레포는 byte-identical. · A `--skills` flag (also emitted for `--agent claude|cursor`); opt-in, preview-first, never-overwrite, recognize-don't-run; byte-identical when not requested.

## 참고 · Notes

- 본문은 기존 `src/task-prompts.js` 워크플로를 재사용하고, 생성 머신의 절대경로(사용자명 포함)를 넣지 않도록 workspace 참조를 저장소-상대(`.`)로 둔다. · Bodies reuse `src/task-prompts.js` and keep the workspace reference repo-relative (no absolute machine path / username baked into a committed artifact).
- 에이전트-중립 프롬프트는 검증 대상 트리(`docs/llm-wiki/`) 밖(`.llm-wiki/prompts/`)에 두어 어댑터 파일과 동일하게 취급한다(게이트 초안의 `docs/llm-wiki/prompts/`에서 조정). · The neutral prompt lives outside the validated `docs/llm-wiki/` tree (`.llm-wiki/prompts/`), treated like the adapter files (a refinement of the gate draft's path).
- 검증: node --test 231 통과(신규 2), validate result:pass 0 findings, strict pass. `init --write --agent claude --skills`로 9개 아티팩트 생성 + 도메인 맵 주입 + 절대경로 미유출 CLI 확인. · Verify: 231 tests pass (+2), validate result:pass (0 findings), strict pass; 9 artifacts + domain-map injection + no path leak confirmed via the real CLI.
- 범위: `GATE_REVIEW.md` Gate 21(accepted_for_1.15.0). · Scope: GATE_REVIEW Gate 21.
