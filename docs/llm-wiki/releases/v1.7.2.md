---
title: 릴리스 노트 v1.7.2 · Release Notes v1.7.2
tags:
  - llm-wiki
  - release-notes
status: needs_review
doc_type: release_notes
project: llm-wiki-standard
last_updated: 2026-07-15
author: ai-generated
last_edited_by: Claude Code
wiki_block_version: v1
source_files:
  - package.json
  - src/cli.js
  - src/index.js
  - src/commands.js
  - src/mcp/dispatch.js
related:
  - docs/llm-wiki/log.md
visibility: internal
contains_sensitive_info: false
---

# 릴리스 노트 v1.7.2 · Release Notes v1.7.2

_생성일 2026-07-15. 사람 검토 전 `needs_review`. · Generated 2026-07-15; `needs_review` pending human review._

## 요약 · Summary

- config 스키마 확장(Gate 13)을 위한 enabling-prep. Additive·하위호환 — CLI·JSON·프로그래매틱 API·frontmatter 계약과 zero-dep 불변. · Enabling prep for config schema growth (Gate 13). Additive/backward-compatible; 1.0.0 contracts and the zero-dependency policy are unchanged.

## 추가 · Added

- `resolveOptions(overrides)` — `normalizeOptions`의 config 인식 async 동반자. `llm-wiki.config.json`을 CLI처럼 병합해 `{ options, errors }` 반환. 동기 `normalizeOptions`·동결 `commands` 맵 불변. · a config-aware async companion to `normalizeOptions` that merges `llm-wiki.config.json` and returns `{ options, errors }`; the sync `normalizeOptions` and frozen map are unchanged.
- `init`/`quickstart --write`가 최소 `llm-wiki.config.json`을 scaffold(감지 type·선택 agents 시드, preview-first, 기존 파일 미덮어씀). · init/quickstart scaffold a minimal config, preview-first and never overwriting an existing one.
- `doctor`가 effective config를 echo(`present (type=..., agents=...)` 또는 `present (invalid: N errors)`). · doctor echoes the effective config.

## 변경 · Changed

- config 로딩을 command layer 아래로 이동 — MCP `tools/call`도 프로젝트 config를 병합(malformed는 `isError`)해 CLI·API·MCP가 동일 effective options를 해석한다(이전엔 CLI만; Gate 11 honest limit). · config loading moved below the command layer so MCP also merges project config; all three surfaces now resolve the same effective options.

## 참고 · Notes

- 명시/CLI 값이 이기고 config는 미설정 항목만 채우며 `strict`는 additive. `1.8`이 스키마를 확장하기 전 실사용 축적용 enabling-prep다(범위: GATE_REVIEW Gate 13, proposed). · Explicit/CLI values win; config only fills unset fields and adds strict. This is the enabling prep before 1.8 grows the schema (Gate 13, proposed).
- 사람 검토 전까지 `needs_review`로 유지한다. 이 릴리스로 지식 문서 3개(PUBLIC_API/DOMAIN_FEATURES/ARCHITECTURE_CONVENTIONS)가 doc-sync로 needs_review가 됐다(배포 후 재검토 대상). · Kept `needs_review`; three knowledge docs dropped to needs_review via doc-sync (re-verify after deploy).
