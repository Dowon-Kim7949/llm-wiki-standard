---
title: 릴리스 노트 v1.15.1 · Release Notes v1.15.1
tags:
  - llm-wiki
  - release-notes
status: needs_review
doc_type: release_notes
project: llm-wiki-standard
last_updated: 2026-07-21
author: ai-generated
last_edited_by: Claude Code
wiki_block_version: v1
source_files:
  - package.json
  - src/commands.js
related:
  - docs/llm-wiki/log.md
visibility: internal
contains_sensitive_info: false
---

# 릴리스 노트 v1.15.1 · Release Notes v1.15.1

_생성일 2026-07-21. 사람 검토 전 `needs_review`. · Generated 2026-07-21; `needs_review` pending human review._

## 요약 · Summary

- 스킬 생성 온보딩 수정. **dogfood**: 이 변경은 1.15.0이 만든 `/llm-wiki-feature` 스킬을 이 저장소 자신에게 실행해서 구현했다. 계약·zero-dep 불변. · Skill-generation onboarding fix, implemented by running the tool's own `/llm-wiki-feature` skill on this repository. Contracts and zero-dep unchanged.

## 변경 · Changed

- **스킬 생성 시 "에이전트 재시작 필요" 안내.** Claude Code는 스킬을 세션 시작 시점에 로드(hot-reload 아님)하므로, `--skills`로 갓 만든 스킬의 `/llm-wiki-*` 명령은 재시작 전까지 "unknown"이다. `init`/`quickstart --write`가 스킬을 실제로 생성했을 때만 이중언어 안내 한 줄을 출력한다(`src/commands.js`의 `SKILL_RELOAD_NOTE` + `initWrite`/`quickstartInitSummary`). · A restart-required note when skills are generated, shown only when a skill was actually created.

## 참고 · Notes

- 실사용 마찰(사용자가 스킬 생성 직후 `/llm-wiki-feature`를 쳤다가 "unknown"을 만남) 기반 수정 — 도구가 자기 스킬로 자기 마찰을 고친 루프. · Driven by a real friction the maintainer hit; the tool fixed its own onboarding friction using its own skill.
- 이 저장소는 이제 자기 스킬 아티팩트(`.claude/skills/`·`.cursor/rules/`·`.llm-wiki/prompts/`)를 dogfood로 커밋해 추적한다(npm `files` allowlist 밖이라 패키지엔 실리지 않음). · The repo now commits its own generated skill artifacts as dogfood (outside the npm `files` allowlist, so they do not ship in the package).
- `DOMAIN_FEATURES`는 스킬(에이전트) 편집으로 `needs_review`로 강등됐다가 사람 검토(Dowon-Kim, 2026-07-21) 후 `verified`로 재승인됐다. · DOMAIN_FEATURES was downgraded by the agent edit, then re-verified after human review.
- 검증: node --test 232 통과(신규 1), validate result:pass 0 findings, strict pass. · Verify: 232 tests pass (+1), validate result:pass (0 findings), strict pass.
